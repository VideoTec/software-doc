http://www.jb51.net/article/32866.htm

以:打头的单个的:
表示该行是一个标签，
它之后的内容是一个标签段，
如:test，则表示:test之下的内容是标签段，而test是这个标签段的名，
1）可以用 goto test 、goto :test 跳转到该标签段
2）或用 call :test 调用该子过程

特殊情况：
1）冒号不能当标签名，即 :: 不能用于表示一个标签段
2）:加上空格 起到注释的作用