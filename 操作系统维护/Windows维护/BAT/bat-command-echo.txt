echo[英][ˈekəʊ] [美][ˈekoʊ]
n.回声，共鸣；（言语、作风、思想等的）重复；重复者；[无线电]回波

《@echo off》
如果想关闭“ECHO OFF”命令行自身的显示，则需要在该命令行前加上“@”

--------------------------------------------
https://zh.wikipedia.org/wiki/Echo_(%E5%91%BD%E4%BB%A4)
--------------------------------------------
在计算机技术中，echo是一个DOS、OS/2、Microsoft Windows、Singularity、Unix和类Unix系统中的命令，
用来将字符串输出到终端上。
它通常在shell脚本和批处理中使用，以将状态信息输出到屏幕或文件中

--------------------------------------------
echo off
--------------------------------------------
DOS、Microsoft Windows支持选项on和off。
例如，echo off可以关闭回显，即不显示命令提示符

一般，批处理会在首行执行此条命令，以关闭执行过程中的回显。
然而，执行此条命令亦会显示命令提示符，需使用@echo off以关闭该条命令的回显

--------------------------------------------
echo 输出中文 为什么会出错
--------------------------------------------
@echo off
echo 请按回车键
运行出现下面的错误

'请按回车键' 不是内部或外部命令，也不是可运行的程序
或批处理文件。
请按任意键继续. . .

最后找到原因是，换行符的问题，必须要用 CRLF